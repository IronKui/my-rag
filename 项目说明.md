# 项目说明文档：AI知识库问答Agent

**版本**：V3.1  
**日期**：2026年8月25日  
**状态**：企业级 RAG 知识库，核心骨架 + 文件管理已完成，进入补齐完善阶段  

---

## 1. 项目概述

### 1.1 项目名称

AI知识库问答Agent

### 1.2 项目背景
基于LangChain框架，构建一个支持知识库问答、主动出题、角色扮演的智能Agent系统。用户可上传个人资料（笔记/文档/图片），通过AI实现智能问答和主动出题功能，覆盖学习巩固、面试模拟、角色扮演等应用场景。

### 1.3 项目目标（V3.0 更新）
**定位：企业级 RAG 知识库**。不做简单学习 Demo，每个技术决策按企业级标准对齐：

- **核心目标**：构建可对标企业级实践（RAGFlow、Dify 等）的 RAG 知识库系统
- **已实现**：账号系统、用户隔离、记忆持久化、会话管理、运行时上下文注入、文件可见范围、溯源去重
- **文档解析**：结构感知切分、版面分析（表格/代码块/双栏/跨页）、OCR、多模态兜底（待做）
- **检索质量**：混合检索（向量 + 关键词）、rerank 重排（待做）
- **数据安全**：账号系统 ✅、知识库用户隔离 ✅、上传安全（待做）
- **可靠性**：记忆持久化 ✅、会话管理 ✅、统一错误处理（待做）
- **技术栈**：Python + FastAPI + LangChain 后端，HTML 前端（Vue 待做）
- **可选**：部署给朋友使用（Docker + 云服务器）

---

## 2. 核心设计思路

### 2.1 传统RAG vs Agent系统

```
传统RAG：
用户上传 → 向量化 → 用户提问 → 检索 → 生成回答

Agent系统：
用户上传 → 向量化 → 用户提问 → Agent判断 → 调用对应Tool
                                              ├── 检索知识库 → 回答
                                              ├── 出题考用户
                                              ├── 角色扮演对话
                                              └── 直接回答
```

### 2.2 Agent + 工具调用设计

```
Agent（大脑）
├── 判断用户意图
├── 决定调用哪个Tool
└── 组织最终回答

Tools（工具）
├── retrieve_tool：检索知识库，回答问题
├── quiz_tool：根据知识库出题
├── evaluate_tool：评判用户答案
└── roleplay_tool：角色扮演对话

Memory（记忆）
└── ConversationBufferMemory：多轮对话上下文
```

---

## 3. 功能需求

### 3.1 文件上传与解析
- **输入**：支持 `.pdf`、`.docx`、`.md`、`.txt`、`.png`、`.jpg`、`.jpeg` 格式
- **处理**：
  - 文本类文件：LangChain DocumentLoader提取文字
  - 图片文件：OCR识别提取文字
    - 开发阶段：pytesseract（需本地安装Tesseract引擎）
    - 生产阶段：百度OCR API（免费每天500次）或阿里云OCR
- **输出**：解析后的纯文本内容，供后续向量化存储

### 3.2 知识库构建（向量化存储）
- 将解析后的文本切分为语义块（chunk）
- 调用Embedding模型将文本块向量化
- 存入ChromaDB向量库

### 3.3 智能问答（用户问 → 系统答）
- 用户输入自然语言问题
- Agent判断调用retrieve_tool
- 基于知识库进行向量检索，召回相关文本片段
- 将检索结果和对话历史组合成Prompt，调用大模型生成回答

### 3.4 主动出题（系统问 → 用户答）
- Agent判断调用quiz_tool
- 系统根据知识库内容，自动生成题目
- 用户提交答案后，Agent调用evaluate_tool评判
- 给出得分和解析反馈

### 3.5 模式切换（通过Prompt实现）
- **学习模式**：系统扮演"老师"，答疑解惑，出题检测
- **面试模式**：系统扮演"面试官"，追问职业/技能问题
- **角色扮演模式**：系统扮演用户指定的任意角色，进行开放式对话

> 模式切换仅改变系统Prompt，不涉及底层逻辑变更。

### 3.6 对话记忆
- 同一会话内支持多轮对话上下文关联
- 使用LangChain ConversationBufferMemory

---

## 4. 技术选型

| 模块 | 选型 | 理由 |
|:---|:---|:---|
| AI编排框架 | LangChain | 练习目标，社区活跃 |
| 后端框架 | FastAPI | 已学过，异步高性能 |
| 前端框架 | Vue 3 + Naive UI | 前后端分离，清新风格 |
| 向量数据库 | ChromaDB（内存版） | 轻量、无服务器依赖 |
| Embedding模型 | BAAI/bge-base-zh-v1.5 | 中文场景主流选择，效果更好 |
| 大语言模型 | DeepSeek | 已有API Key，兼容OpenAI格式 |

---

## 5. 项目结构

```
langchain-learn/
├── backend/                    # 后端（FastAPI + LangChain）
│   ├── app.py                  # FastAPI主入口
│   ├── core/
│   │   ├── __init__.py
│   │   ├── agent.py            # Agent定义（LangChain Agent）
│   │   ├── tools.py            # 工具定义（retrieve/quiz/evaluate）
│   │   ├── rag.py              # RAG流程（文档解析/向量化/检索）
│   │   ├── memory.py           # 对话记忆
│   │   └── prompts.py          # Prompt模板
│   ├── uploads/                # 临时上传目录
│   ├── requirements.txt
│   └── .env                    # API Key
│
├── frontend/                   # 前端（Vue 3 + Naive UI）
│   ├── src/
│   │   ├── App.vue             # 主组件
│   │   ├── components/
│   │   │   ├── ChatPanel.vue   # 聊天面板
│   │   │   ├── FileUpload.vue  # 文件上传
│   │   │   └── QuizPanel.vue   # 出题面板
│   │   ├── api/
│   │   │   └── index.js        # 接口调用
│   │   └── main.js             # 入口文件
│   ├── package.json
│   └── vite.config.js
│
└── README.md
```

---

## 6. 接口定义

### 6.1 上传接口
- **URL**：`POST /api/upload`
- **请求**：FormData（file + session_id）
- **响应**：
```json
{"code": 0, "message": "上传成功", "data": {"file_name": "xxx.pdf", "chunk_count": 15}}
```

### 6.2 对话接口
- **URL**：`POST /api/chat`
- **请求**：
```json
{"question": "...", "session_id": "...", "mode": "learning"}
```
- **响应**：
```json
{"code": 0, "data": {"answer": "回答内容", "sources": ["片段1"]}}
```

### 6.3 出题接口
- **URL**：`POST /api/quiz`
- **请求**：
```json
{"session_id": "...", "mode": "learning"}
```
- **响应**：
```json
{"code": 0, "data": {"question": "题目内容", "question_id": "q1"}}
```

### 6.4 评判接口
- **URL**：`POST /api/evaluate`
- **请求**：
```json
{"session_id": "...", "question_id": "q1", "user_answer": "..."}
```
- **响应**：
```json
{"code": 0, "data": {"score": 80, "feedback": "评价内容"}}
```

### 6.5 清空会话
- **URL**：`POST /api/clear`
- **请求**：
```json
{"session_id": "..."}
```
- **响应**：
```json
{"code": 0, "message": "会话已清空"}
```

---

## 7. LangChain核心组件使用

### 7.1 Agent（ReAct模式）
```python
from langchain.agents import AgentExecutor, create_react_agent
from langchain_core.prompts import ChatPromptTemplate

# 定义Agent
agent = create_react_agent(llm, tools, prompt)
agent_executor = AgentExecutor(agent=agent, tools=tools, memory=memory)
```

### 7.2 Tool（工具定义）
```python
from langchain.tools import tool

@tool
def retrieve_from_knowledge_base(query: str) -> str:
    """从知识库检索相关信息"""
    # 检索逻辑
    return results

@tool
def generate_quiz(topic: str) -> str:
    """根据知识库内容出题"""
    # 出题逻辑
    return question
```

### 7.3 Memory（对话记忆）
```python
from langchain.memory import ConversationBufferMemory

memory = ConversationBufferMemory(return_messages=True)
```

### 7.4 RAG（检索增强生成）
```python
from langchain_community.vectorstores import Chroma
from langchain_community.embeddings import HuggingFaceEmbeddings

# 向量化存储
embeddings = HuggingFaceEmbeddings(model_name="shibing624/text2vec-base-chinese")
vectorstore = Chroma.from_documents(chunks, embeddings)
retriever = vectorstore.as_retriever()
```

---

## 8. 应用场景

| 场景 | 系统角色 | 用户角色 | Prompt示例 |
|:---|:---|:---|:---|
| 学习助手 | 老师 | 学生 | "你是老师，根据知识库回答问题，并出题检测学生掌握程度" |
| 面试练习 | 面试官 | 求职者 | "你是面试官，根据知识库提问，追问细节，给出评价" |
| 角色扮演 | 任意角色 | 对话者 | "你是{角色}，基于知识库内容进行角色扮演对话" |

---

## 9. 验收标准

| 编号 | 验收项 | 通过标准 |
|:---|:---|:---|
| A01 | 文件上传 | 支持PDF/Word/MD/图片，上传后返回成功 |
| A02 | 知识库问答 | 上传后提问，回答与上传内容相关 |
| A03 | 主动出题 | 系统能根据知识库出题，评判用户答案 |
| A04 | 模式切换 | 切换模式后，系统语气和行为变化 |
| A05 | 对话记忆 | 连续追问时，能引用前文内容 |
| A06 | Web界面 | 前后端分离，Vue3 + Naive UI，交互流畅 |

---

## 10. 部署方案

| 阶段 | 方案 | 说明 | 费用 |
|:---|:---|:---|:---|
| 开发阶段 | 本地运行 | FastAPI + Vue本地联调 | 免费 |
| 临时分享 | ngrok / Cloudflare Tunnel | 内网穿透，生成公网链接，朋友直接访问 | 免费 |
| 正式部署 | Docker + 云服务器 | 企业级部署方式，需要域名+HTTPS | 付费 |

---

## 11. 完善规划（当前阶段：企业级核心骨架已完成，进入补齐完善）

> MVP 基础功能 + 企业级核心骨架已跑通。
> 以下规划按企业级标准，列出已完成项和剩余项。

### 11.1 功能模块（已完成 vs 待做）

| 优先级 | 模块 | 说明 | 状态 |
|:---|:---|:---|:---|
| 高 | 多模式切换 | 学习/面试/角色扮演接入 agent | ✅ 完成（8-7） |
| 高 | 知识库账号隔离 | 用户A的文件仅用户A可检索 | ✅ 完成（8-10） |
| 高 | 账号系统 | 注册/登录/JWT/用户隔离 | ✅ 完成（8-17） |
| 高 | 记忆持久化 | SqliteSaver，重启不丢 | ✅ 完成（8-14） |
| 高 | 会话上下文（中期记忆）| SummarizationMiddleware 摘要压缩 | ✅ 完成（8-15） |
| 高 | 运行时上下文注入 | context_schema + ToolRuntime，缓存不膨胀 | ✅ 完成（8-19） |
| 高 | 文件可见范围 | 账号级/会话级 | ✅ 完成（8-19） |
| 高 | 会话管理 + 历史加载 | sessions/messages 双存储 | ✅ 完成（8-19） |
| 高 | **文件管理** | 用户查看/删除上传的文件（user_files 表替代 JSON）| ✅ 完成（8-25） |
| 中 | **上传安全** | 文件类型/大小校验 | ⬜ 待做 |
| 中 | 前端整体规划 | Vue3 + Naive UI | ⬜ 待做 |

### 11.2 细节优化（内功）

| 优先级 | 优化项 | 现状 | 目标 |
|:---|:---|:---|:---|
| ✅ | 溯源 | 检索结果带文件名 | ✅ 完成（8-10） |
| ✅ | 文件去重 | 硬重复拦截 + 同名重命名 | ✅ 完成（8-10） |
| ✅ | 记忆持久化 | SqliteSaver 重启不丢 | ✅ 完成（8-14） |
| ✅ | 会话压缩 | SummarizationMiddleware | ✅ 完成（8-15） |
| 中 | 检索质量 | 纯向量检索 | 混合检索（BM25 + 向量）、rerank 重排 |
| 中 | 知识库语义切分 | 字符硬切 | 结构感知切分（大工程）|
| 中 | 错误处理 | 部分接口无统一异常处理 | 统一错误响应格式 |
| 中 | 上传安全 | 无文件校验 | 文件类型白名单、大小上限 |
| ✅ | 文件管理 | user_files 表 | 用户查看/删除文件（8-25 完成）|
| 📝 | 冷启动并发 | 偶发报错 | 加锁（已做）/ 启动预热 |
| 📝 | Summarization bug | 偶发 tool_calls 报错 | 进阶修复 |
| 低 | 图片 OCR | load_image 未实现 | pytesseract/多模态 |
| 低 | 流式输出 | 一次性返回 | SSE 打字机效果 |

### 11.3 三层记忆架构

```
短期记忆（当前会话）✅ 已完成：SqliteSaver 持久化 + messages 表双存储
中期记忆（会话内压缩）✅ 已完成：SummarizationMiddleware 摘要压缩
长期记忆（跨会话画像）⏳ 未来：账号系统已就绪，可做用户偏好/画像
```

### 11.4 推荐实施顺序

```
✅ 已完成：多模式切换、账号隔离、账号系统、记忆持久化、会话压缩、
          运行时注入、文件可见范围、会话管理、溯源去重、文件管理
⬜ 剩余实施顺序：
第1步：上传安全（文件类型/大小校验）        （工程健壮性，当前）
第2步：混合检索 + rerank                    （检索质量）
第3步：统一错误处理                         （工程健壮性）
第4步：前端整体规划（Vue）                  （界面框架）
第5步：知识库语义切分（结构感知）           （检索质量，大工程）
第6步：长期记忆（用户画像）                 （跨会话偏好）
第7步：冷启动预热 + Summarization bug 修复  （稳定性）
第8步：图片 OCR / 流式输出                  （锦上添花）
```

### 11.5 发布计划

| 阶段 | 动作 | 说明 |
|:---|:---|:---|
| 当前 | 仓库保持私有 | 完善过程中不公开 |
| 完善后 | 公开仓库 | 代码达到可上线标准后转为公开 |
| 可选 | 朋友本地运行 | 提供 README、.env.example、模型下载说明 |

---

**文档结束**
